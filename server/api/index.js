// const router = require('express').Router()
// const {Sprite, Frames, Layers} = require('../db/models')

//Find sprite by hash
// router.get('/:hash', async (req, res, next) => {
//   try {
//     const selectedSprite = await Sprite.findOrCreate({
//       hash: req.params.hash
//     })
//   } catch (err) {
//     next(err)
//   }
// })

//Error handling
// router.use((req, res, next) => {
//   const error = new Error('Not Found')
//   error.status = 404
//   next(error)
// })


// module.exports = router
